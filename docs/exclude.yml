# Hand-maintained list of fnmatch globs against `org/name` slugs.
# Slugs matching any pattern here are dropped during scraping.

- bauerjustin/*
- cookbook/test
- harbor/hello-world
- NovitaAI/tb21-*
- openthoughts/*
- shogunpurple/9CBCC967-BDAF-43D0-9485-52EBBE4A5094
# Verbatim copy of agentscope-ai/pawbench (identical 150 inputs, renamed
# sample ids); keep only the canonical author org.
- xiaoboai/pawbench
# Individual's experimental personal tasks, not a published benchmark
# (e.g. task1_v3_1_stock_research_basket_rolling).
- ashantanu/*
# Experimental Modal-platform port iterations ("patched verifier rebuild",
# train/eval splits); not a published benchmark and not a real org. The
# canonical vmax dataset is kept as vmax/vmax-tasks.
- vmax-modal/*
# Verbatim duplicate of orinlabs/horizon-public (identical 3-task public
# example set, same sample ids); keep the canonical name matching the
# orinlabs/horizon repo and "Horizon" brand.
- orinlabs/horizon-1-public
# Individual's (handle-named) project with no public benchmark repo, no
# paper, and no external footprint; all the user's repos are forks of other
# projects. Includes a -trial upload-test subset. Not a published benchmark.
- tmax/*
# No public provenance: the GitHub user/org `svgap` doesn't exist, there is
# no paper or repo, and the hub page carries no author metadata (8-sample
# SystemVerilog reset-release micro-benchmark). Not a published benchmark.
- svgap/*
# Individual's (User, not org) A/B experiment pairing SkillsBench control and
# treatment arms (10 tasks / 20 samples); no lamina-bench repo exists on
# GitHub and no paper or external footprint. Not a published benchmark.
- aryaniyaps/*
# Individual's (User, not org) experimental IT-support mock-provider agent
# tasks (5 samples with gappy ids a-1..a-4, a-8; fake ServiceNow/Okta APIs);
# no task-aw repo exists on GitHub and no paper or external footprint. Not a
# published benchmark.
- abhishek203/*
# Timestamped terminal-bench CI/upload-test scratch orgs (e.g.
# tbench-up4-a-up4_1784108341_146593/up4-dataset); dev artifacts, not
# benchmarks. The underscore before the slash keeps this from matching a
# hypothetical legitimate tbench-* org.
- tbench-*_*/*
# Re-upload of Harvey LAB by an individual (User, not org) from a fork of
# harveyai/harvey-labs: all 1,251 task ids of the canonical harveyai/lab are
# a strict subset of its 1,760 (extra "scenario" variants, non-canonical
# rewardkit/OpenRouter grading). Keep the author org's harveyai/lab.
- punitarani/harvey-labs
# Verbatim duplicate of userbench/UserBench (identical 1,520 inputs AND
# sample ids) under the project's superseded name — the UserBench desc says
# "Formerly developed as SWESimBench v2". userbench/UserBench is kept
# (categorized in overrides.yml); this stale-name copy goes regardless.
- swesimbench/SWESimBench
# 1-task subset re-upload of ale/rsi-post-training by the same publisher two
# days later (identical input to ale/olmo3-7b-zero-rl-acc, same verbatim
# desc); the rsi-index GitHub org has no public repos. Keep the complete
# 6-task ale/rsi-post-training. Revisit if rsi-index becomes the canonical
# home of the ALE RSI benchmark.
- rsi-index/post-training
# The Lamina "Headless Product Design skill" A/B experiment again (same
# project already excluded as aryaniyaps/*): 12 samples = 4 toy apps x 3
# arms (direct/plan/lamina) with dev-*-v3 scratch task ids. The lamina
# GitHub org has 0 public repos and there is no paper or external
# footprint. The tasks are also multi-step task.toml, which inspect_harbor
# can't load (NotImplementedError). Revisit if Lamina publishes a real
# benchmark repo/paper and multi-step tasks become supported.
- lamina/*
# Individual's (User, not org) dev pilot of the same Lamina experiment —
# identical 12 task ids to lamina/product-bench, slug references a working
# issue number ("lb6-dev-pilot-issue18"), and the desc itself says
# "Development-only; not confirmatory". Not a published benchmark.
- shiv-eshwar/*
# Individual's (User, not org) 2-sample compute-markets experiment matching
# their "the-compute-bazaar" repo ("tinkering with compute markets"); no
# benchmark repo, paper, or external footprint. Not a published benchmark.
- gustofied/*
# Individual's (User, not org) Kubernetes-sandbox ports of benchmarks we
# already list canonically (arc-agi-2-k8s → arcprize/arc-agi-2, dbt-labs-k8s
# → dbt-labs/ade-bench); the descs say "adapted for Kubernetes sandbox
# execution". Derivative infra re-uploads, not new benchmarks.
- titouanlne/*
# Individual's (User "Nanotek", 0 repos) leaderboard doc for their personal
# models: the package holds a single sample whose input says "This is a
# documentation placeholder, not a runnable benchmark task." The underlying
# tasks are the standard SWE-bench Verified instances, already listed
# canonically as swe-bench/swe-bench-verified.
- nanoswe/*
# Internal test zoo for "Klyrune", Polyvor Labs' unreleased agentic CLI tool.
# Three uploads so far, none a published benchmark: klyrune-bench (100
# trivial synthetic tasks — echo an exact string to answer.txt, one-function
# fixes like rgb_to_hex/safe_int; excluded 2026-08-12), cyberdefense-bench
# (50 templated single-function "patch <func> to be safe" micro-tasks for
# Klyrune's blue-team loop), and revbench (22 third-party CTF challenges —
# LKSN{} flag format, external challenge authors credited in the prompts —
# repackaged with no provenance or licensing). The org's only related repo
# (Polyvor-Labs/Klyrune-Benchmarks, 0 stars) holds AutoCodeBench leaderboard
# results for Klyrune, not these benchmarks; Klyrune itself is still
# unreleased. Revisit if Klyrune ships publicly with real benchmark repos.
- polyvorlabs/*
# 1-sample per-case slices of itmo-autods/scientific-cases (verified: the
# aggregate's three sample inputs are byte-identical to the three singles).
# Whatever is decided about the parent scientific-cases dataset, listing
# the single-sample slices separately adds only noise.
- itmo-autods/fdata-exit-case
- itmo-autods/maize-yield-case
- itmo-autods/openpoly-tg-case
# Feature smoke-test suite for the Open Design tool (nexu-io/open-design, the
# open-source "Claude Design alternative"), not a published benchmark: 7 tasks,
# one per tool feature category (landing page, dashboard, mobile onboarding,
# text layout, PPT report, multi-format export, incremental patch) — the
# landing-page task even asks for a page "for a brand called Nexu". Uploaded
# by hub user chenai313 (GitHub account created 2026-07-28, 0 repos, no
# verifiable nexu-io affiliation); no benchmark repo anywhere in the nexu-io
# org, no paper, no announcement, zero search footprint for an "Open Design
# Benchmark". The hub org name is also unrelated to GitHub's open-design org
# (an FPGA/electronics group). Exact slug, not org-wide: revisit if nexu-io
# ships a real, publicly documented benchmark under this name.
- open-design/open-design
# Internal dev artifacts of Frontis, an agent-observability product, with no
# public provenance: the frontis GitHub org has existed since 2018 with zero
# public repos and no verifiable affiliation; no benchmark repo, paper, or
# announcement exists anywhere. agent-obs-core is 3 samples with internal
# test ids (B02/B08/B08b) and working-version churn ("minor environment
# update from v3.1.1"); generated-basic-production-traces self-describes as
# "mechanically generated from Frontis production Historical Trials" ("MVP
# Harbor Dataset") — a replay-pipeline artifact, not a benchmark. Revisit if
# Frontis publishes a real benchmark repo/paper.
- frontis/*
# Individual's (User, bio "i have no idea what i am doing") personal
# self-consistency experiment, not a published benchmark:
# rating-generation-consistency's 45 tasks = 3 whimsical "neutral topics"
# (toilet-paper orientation, hot-dog-sandwich classification, restaurant
# etiquette) x 3 rating scales x 5 target stances. No repo, paper, or
# announcement anywhere; the account's only Harbor-related repo is a personal
# mirror of standard Harbor datasets that doesn't contain this one. Revisit
# if it is ever published as a real benchmark.
- adameubanks/*
# Verbatim duplicate of dbt-labs/ade-bench (identical 48 task inputs) by an
# individual (User, not org), self-described as "ADE-Bench adapted to run
# with Inspect Harbor on K8s" — the same derivative infra re-upload class as
# titouanlne/*. Keep the canonical author org's dbt-labs/ade-bench.
- tomseimandi/ade-bench
# Personal staging/test uploads by a Terminal-Bench core maintainer (User,
# not org), both carrying the canonical Terminal-Bench desc verbatim:
# tb-test is a 1-sample upload test; tb4-preview ("TB4" preview, 66 tasks)
# doesn't load (build.no_cache rejected by inspect_ai's ComposeConfig).
# Canonical releases live under terminal-bench/. Revisit if a real personal
# benchmark appears under this account.
- ryanmarten/*
# TEMPORARILY excluded (would otherwise be kept): four legitimate-looking
# Blobfish AI benchmarks that fail to load because inspect_ai's ComposeConfig
# rejects docker-compose long-form volume mounts
# (services.main.volumes.0: Input should be a valid string). Re-include and
# categorize once inspect_ai supports them (fix proposed upstream):
# counselbench-100 ~[Law, Professional], litigation ~[Law, Professional],
# salesbench-100 ~[Assistants, Professional], ledgerbench-100 ~[Finance,
# Professional] (source repo blobfishai/finance-world). When re-including,
# also check whether counselbench-100 duplicates litigation (both 100
# long-horizon legal-agent tasks) before listing both.
- blobfishai/counselbench-100
- blobfishai/ledgerbench-100
- blobfishai/litigation
- blobfishai/salesbench-100
# TEMPORARILY excluded (would otherwise be kept): canonical org upload of the
# living Terminal-Bench-Science benchmark (70 tasks,
# https://github.com/harbor-framework/terminal-bench-science) — distinct from
# the google-deepmind/terminal-bench-science 52-task pinned snapshot, which
# stays listed. Fails to load: inspect_ai's ComposeConfig rejects pids_limit.
# Re-include once supported (fix proposed upstream) with overrides:
# categories ~[Science, Coding], function_name: terminal_bench_science (dedup,
# no collision), title: Terminal-Bench-Science, repo above.
- terminal-bench-science/terminal-bench-science
# TEMPORARILY excluded (would otherwise be kept): 87 CTF tasks curated from
# the AlibabaCTF competition series by the Alibaba-AAIG org (a real
# Organization) — fails to load because inspect_ai's ComposeConfig rejects
# the `cgroup` service key (one task uses it). Re-include once supported
# (fix proposed upstream in inspect_ai PR #5139) with overrides:
# categories ~[Cybersecurity], title: AlibabaCTF.
- alibaba-aaig/alibaba-ctf
# TEMPORARILY excluded (would otherwise be kept): the verified 40-task split
# of ORCA-bench (root-cause analysis of oncall incidents), uploaded by the
# same canonical org as the listed orca-bench/orca-bench — fails to load
# because inspect_ai's ComposeConfig rejects `stop_grace_period` (all its
# compose files use it). Re-include once supported (fix proposed upstream in
# inspect_ai PR #5139) with overrides mirroring the sibling: categories
# ~[Coding, Professional], function_name: orca_bench_verified (dedup, no
# collision), title: ORCA-bench Verified, repo orca-bench/ORCA-bench,
# arxiv 2607.28545, and a desc override trimming the dangling "please see."
- orca-bench/orca-bench-verified
